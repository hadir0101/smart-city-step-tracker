# Ingestion package — two independent data sources, same detection algorithm.
#
#   csv_ingest.py   — load a Phyphox CSV export and run step detection
#   live_ingest.py  — poll the Phyphox HTTP API live and run step detection
